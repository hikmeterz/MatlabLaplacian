function [A] = bonus(C, M, N)
    % Assume dimensions of C, M, N are given properly
    % Assume C, M, N are full column-rank
    % Assume C is NOT zero matrix
    
    [mC, nC] = size(C);
    [mM, nM] = size(M);
    [mN, nN] = size(N);
    
    % 30 points
    % find A
    A = C;      % NOT correct
end